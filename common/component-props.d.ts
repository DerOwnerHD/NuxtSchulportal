export interface LegacySelectBoxOption {
    id: string;
    text?: string;
    icon?: string[];
    default?: boolean;
}

export interface FluidSelectionOption {
    title: string;
    id: string;
    subtitle?: string;
    default?: boolean;
}
